describe("Checking log + todo flow", () => {
  // Initial
  it("login", () => {
    cy.visit("/")
  })

  // Login correctly
  // Enter text in input
  // List will be of length 0
  // Click add button
  // Item will get added to list
})
